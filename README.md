# Blogicum
1. Активировать venv в корневой папке `. venv/Scripts/activate`
2. Импортировать зависимости в корневой папке `pip install -r requirements`
3. Написать `python blogicum/manage.py runserver`
